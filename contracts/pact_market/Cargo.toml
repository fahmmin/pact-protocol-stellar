[package]
name = "pact_market"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { version = "21.7.6", features = ["testutils"] }

[lib]
crate-type = ["cdylib", "rlib"]
